export default function Loading() {
  return (
    <div className="state-page" role="status" aria-live="polite">
      <div className="state-card">
        <p className="section-label">SYSTEM / INITIALIZING</p>
        <h1>Preparing the signal path.</h1>
      </div>
    </div>
  );
}
